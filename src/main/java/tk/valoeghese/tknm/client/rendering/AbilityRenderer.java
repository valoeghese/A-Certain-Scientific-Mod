package tk.valoeghese.tknm.client.rendering;

import java.util.function.Consumer;

import net.minecraft.client.world.ClientWorld;

public class AbilityRenderer implements Consumer<ClientWorld> {
	@Override
	public void accept(ClientWorld world) {
		/*
		ACertainComponent component = とある科学のモド.A_CERTAIN_COMPONENT.get(entity);
		Ability ability = component.getAbility();

		if (ability != null) {
			ability.getRenderer().render(world, entity, 0);
		}*/
	}
}
